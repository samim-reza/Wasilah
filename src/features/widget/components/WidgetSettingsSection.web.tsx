/**
 * The web has no home screen to put a widget on, and the native widget
 * library must not be bundled for it. Metro picks this file over the
 * native one on the web.
 */
export function WidgetSettingsSection() {
  return null;
}
